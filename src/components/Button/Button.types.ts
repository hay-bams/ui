export type ButtonVariants = 'filled' | 'light' | 'outline' | 'default' | 'subtle'

export enum ButtonVariantsEnum {
  filled = 'filled',
  light = 'light',
  outline = 'outline',
  default = 'default',
  subtle = 'subtle',
}
